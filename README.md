# Harbor & Hearth — React migration

This workspace scaffolds a minimal Vite + React app and will be used to migrate `restaurant-management-ready.html` into React components.

Quick start:

1. Install dependencies:

```bash
npm install
```

2. Run dev server:

```bash
npm run dev
```

Next steps: convert the single-file HTML into React components and migrate JS logic into hooks/state.
